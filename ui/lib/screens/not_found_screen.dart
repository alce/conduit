import 'package:flutter/material.dart';

import '../widgets/layout.dart';

class NotFoundScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Layout(body: Text('Page Not Found'));
  }
}
